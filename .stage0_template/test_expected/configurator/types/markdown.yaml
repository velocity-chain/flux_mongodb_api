root:
  name: markdown
  description: A String of text, at least 1 and no more than 4k characters. May contain markdown, newlines, and tabs.
  type: simple
  schema:
    type: string
    maxLength: 4096
  required: false 